# Tarkov OCR Daemon

Автоматическая система распознавания предметов в игре Escape from Tarkov с использованием OCR (оптического распознавания символов).

## 🎯 Описание

Tarkov OCR Daemon - это Python-приложение, которое автоматически отслеживает скриншоты игры Escape from Tarkov, извлекает текст с помощью OCR и идентифицирует предметы в игре. Система работает в фоновом режиме и предоставляет данные через WebSocket API для интеграции с другими приложениями.

## 🏗️ Архитектура

### Основные компоненты:

- **Screenshot Watcher** - отслеживает новые скриншоты в папке игры
- **OCR Engine** - извлекает текст из изображений с помощью EasyOCR
- **Fuzzy Matching** - находит наиболее подходящие предметы из базы данных
- **WebSocket Server** - предоставляет API для клиентских приложений
- **Location Finder** - отслеживает смену карты в игре

### Поток данных:

1. **Отслеживание скриншотов** → `watcher.py`
2. **Обработка изображений** → `screenshot_processor.py`
3. **Извлечение текста** → `ocr.py`
4. **Сопоставление предметов** → `fuzz.py`
5. **Получение деталей** → `api/item.py`
6. **Отправка данных** → `ws/dispatcher.py`

## 🚀 Установка и запуск

### Требования

- Python 3.8+
- Escape from Tarkov
- Доступ к папке со скриншотами игры

### Установка зависимостей

```bash
cd apps/ocr-daemon
pip install -r requirements.txt
```

### Настройка

1. Убедитесь, что путь к скриншотам в `tarkov_ocr/core/config.py` соответствует вашей системе:
    ```python
    SCREENSHOTS_PATH = Path.home() / "OneDrive" / "Документы" / "Escape from Tarkov" / "Screenshots"
    ```

2. При необходимости измените путь к логам:
    ```python
    LOGS_PATH = Path("D:/EFT/Logs")
    ```

### Запуск

```bash
cd apps/ocr-daemon
python -m tarkov_ocr.main
```

## 📡 WebSocket API

Сервер запускается на `ws://0.0.0.0:8765` и поддерживает следующие события:

### Подключение
```javascript
const ws = new WebSocket('ws://localhost:8765');
```

### События

#### `item`
Получается при обнаружении нового предмета:
```json
{
  "type": "item",
  "data": {
    "name": "AK-74",
    "basePrice": 45000,
    ...
  }
}
```

#### `location`
Получается при смене карты:
```json
{
  "type": "location",
  "data": {
    "datetime": "2025-07-25_123.45,678.90,101.11",
    "position": [123.45, 678.90, 101.11],
    "quaternion": [0.12, 0.34, 0.56, 0.78],
    "extra": 0.12
  }
}
```

#### `map`
Получается при смене карты:
```json
{
  "type": "map",
  "data": {
    "name": "factory4_day"
  }
}
```

#### `error`
Получается при ошибке:
```json
{
  "type": "error",
  "message": "Error message"
}
```

## 🔧 Конфигурация

Основные настройки в `tarkov_ocr/core/config.py`:

- `CROP_WIDTH` / `CROP_HEIGHT` - размер области вокруг курсора для OCR
- `WS_HOST` / `WS_PORT` - настройки WebSocket сервера
- `CACHE_TTL` - время жизни кэша предметов (24 часа)

## 📁 Структура проекта

```
tarkov_ocr/
├── api/                 # API для работы с данными предметов
├── core/               # Основная конфигурация и утилиты
├── handlers/           # Обработчики событий
│   ├── cropper.py     # Обрезка изображений
│   ├── location.py    # Определение карты
│   ├── mouse.py       # Работа с курсором
│   ├── ocr.py         # OCR движок
│   ├── runner.py      # Запуск фоновых процессов
│   ├── screenshot.py  # Обработка скриншотов
│   └── watcher.py     # Отслеживание файлов
├── utils/             # Вспомогательные функции
├── ws/                # WebSocket сервер
└── main.py           # Точка входа
```

## 🎮 Как это работает

1. **Игра запущена** - система отслеживает папку со скриншотами
2. **Скриншот сделан** - автоматически обрабатывается
3. **OCR анализ** - извлекается текст вокруг курсора
4. **Сопоставление** - найденный текст сравнивается с базой предметов
5. **API запрос** - получаются детали предмета
6. **Broadcast** - данные отправляются всем подключенным клиентам

## 🔍 Возможности

- ✅ Автоматическое распознавание предметов
- ✅ Отслеживание смены карты
- ✅ Real-time WebSocket API
- ✅ Fuzzy matching для точного сопоставления
- ✅ Кэширование данных предметов
- ✅ Обработка в фоновом режиме

## 🛠️ Разработка

### Добавление новых обработчиков

1. Создайте новый файл в `handlers/`
2. Добавьте функцию в `runner.py`
3. Обновите `main.py` при необходимости

### Расширение API

1. Добавьте новые события в `ws/dispatcher.py`
2. Обновите клиентскую документацию

## 📝 Логирование

Система выводит подробные логи в консоль:
- 📦 Загрузка данных
- 🔄 Фоновые процессы
- 🚀 WebSocket сервер
- 📊 Обработка скриншотов
- 🎯 Результаты распознавания

## 🤝 Интеграция

Система предназначена для интеграции с:
- Electron приложениями
- Web интерфейсами
- Другими игровыми инструментами

## 📄 Лицензия

См. файл LICENSE в корне проекта.
